spring:
    application:
        name: game-api-service
server:
    port: '8200'

    jpa:
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL5Dialect
        hibernate:
            naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
            ddl-auto: update
        show-sql: 'true'
    datasource:
        type: com.zaxxer.hikari.HikariDataSource
        url: jdbc:mysql://125.212.225.166:3306?zeroDateTimeBehavior=convertToNull
        driverClassName: com.mysql.jdbc.Driver
        username: db_cluster
        password: bccs3_maria
        hikari:
            pool-name: maria-db-pool
            maximum-pool-size: 10
            minimum-idle: 5
            max-lifetime: 600000
    cache:
        ehcache:
            config: classpath:ehcache.xml

reflectoring:
    trace: true
